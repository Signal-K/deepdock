---
title: Daily Dashboard
date: 2026-01-05
tags:
  - dashboard
  - daily
icon: lucide//layout-dashboard
---

# 🎯 Daily Dashboard - 2026-01-05

## � Navigation

```dataviewjs
// Get current date from filename
const currentDate = dv.current().file.name;

if (currentDate && currentDate.match(/^\d{4}-\d{2}-\d{2}$/)) {
  // Get all daily files and extract dates from filenames
  const dailyFiles = dv.pages('"content/Operations/Daily/Active"')
    .filter(p => p.file.name.match(/^\d{4}-\d{2}-\d{2}$/))
    .map(p => ({ date: p.file.name, path: p.file.path }))
    .sort((a, b) => a.date.localeCompare(b.date));
  
  const currentIndex = dailyFiles.findIndex(f => f.date === currentDate);
  
  let prevButton = "⬅️ Previous";
  let nextButton = "Next ➡️";
  
  if (currentIndex > 0) {
    const prevFile = dailyFiles[currentIndex - 1];
    prevButton = `[⬅️ ${prevFile.date}](${prevFile.path.replace('content/', '')})`;
  }
  
  if (currentIndex < dailyFiles.length - 1) {
    const nextFile = dailyFiles[currentIndex + 1];
    nextButton = `[${nextFile.date} ➡️](${nextFile.path.replace('content/', '')})`;
  }
  
  dv.paragraph(`${prevButton} | ${nextButton}`);
} else {
  dv.paragraph("⬅️ Previous | Next ➡️");
}
```

## �🚀 Quick Actions

```button
name ➕ Create
type append template
action Create-Task
templater true
class inline
```
```button
name 📋 Organize
type append template
action Organize-Daily
templater true
class inline
```
```button
name 📅 Archive
type append template
action Archive-Daily
templater true
class inline
```
```button
name 🔄 Reset
type append template
action Reset-Dashboard
templater true
class inline
```

```button
name 📊 Projects
type link
action obsidian://open?vault=quartz&file=content/Categories/_Index
class inline
```

---


## 🗓️ Today's Focus Tasks

```dataviewjs
const today = dv.current().date ? dv.current().date.toString().split('T')[0] : null;
if (!today) {
  dv.paragraph('No date set for this page.');
} else {
  let tasks = [];
  for (let page of dv.pages('"content/Categories"')) {
    if (page.file && page.file.tasks) {
      for (let t of page.file.tasks) {
        if (t.text.includes(`#${today}`)) {
          tasks.push(t);
        }
      }
    }
  }
  if (tasks.length === 0) {
    dv.paragraph('No tasks tagged for today.');
  } else {
    dv.taskList(tasks, false);
  }
}
```

## 🧩 In-Progress Story Tasks

```dataviewjs
// Stories in progress with no tasks
// Find story pages under content/Categories and filter those marked in-progress
const pages = dv.pages("\"content/Categories\"").filter(p => p.file && p.file.path && p.file.path.includes('/Stories/'));
const allStories = pages.filter(p => p.status && String(p.status).toLowerCase() === 'in-progress');

// Collect pages that actually have tasks
const withTasks = new Set(pages.filter(p => p.file && p.file.tasks).map(p => p.file.path));
const noTaskStories = allStories.filter(p => !withTasks.has(p.file.path));

dv.header(3, "📝 In-Progress Stories (No Tasks)");
if (!noTaskStories || noTaskStories.length === 0) {
  dv.paragraph("No in-progress stories without tasks.");
} else {
  dv.header(4, "Stories in progress (no tasks defined):");
  noTaskStories.forEach(p => dv.paragraph(p.file.link));
}
```

## 📅 Recent

```note-gallery
path: content/Operations/Daily/Active
query: 'path:content/Operations/Daily/Active/'
recursive: false
limit: 4
sort: desc
sortBy: mtime
fontSize: 8pt
showTitle: true
breakpoints:
  default: 4
  1500: 3
  1000: 2
  700: 1
```

---

## �📥 Today's Notes Dump

### Quick Thoughts
So didn't really do a huge amount of planning for really anything yesterday. Today, things will probably be a little bit different.

On the A&S front, I've been blown away with how expensive the next order could be. Prioritising cheaper movements would be the first step...I think? Need to get it over to Gary and add some urgency for some of the parts. As well as do up an official invoice? I don't think the current homepage for `aswatch.ch` looks good enough and I don't really want to deal with negative feedback this early in the week.

For #Weathr, I will use v0.dev to come up with some changes/additions to the design...the negative indictor is horrible right now. I'll attach screenshots of the current layout so that v0 knows what I'm working with....and then make sure that copilot can convert it into rn code. I obviously need to call Apple Developer support regarding my D-U-N-S number as well. That's hot on the agenda.

I'm itching to get back into the Crashlandings tutorials; this way if I do end up meeting up with Fred I'll have more to show him. Which will obviously help with figuring out what to do next. Just like usual, not really anything to plan here. Yet.

I'm not super happy that I'm behind on Bumble...but I don't think there's a huge amount (read: there's NOTHING!) to plan for this either.

I also have that new contract to look over...

### Meetings & Conversations




### Ideas & Brainstorming
Just thinking about things - MSM + cozy game??? lol

Possibly, after [finally] releasing a demo, let's suggest adding upgradeable/expandable hives as a feature at level 6 or 7?



---

## Routed Notes
- [[content/Categories/Ideas/Star-Sailors-Ecosystem/Bumble/Daily-Insights/2026-01-05.md|Routed: Bumble Ideas]]

- [[content/Categories/Ideas/Weathr/Daily-Insights/2026-01-05.md|Routed: Weathr Ideas]]
- [[content/Categories/Docs/General-Operations/Daily-Notes/2026-01-05.md|Routed: General-Operations Docs]]
- [[content/Categories/Docs/Star-Sailors-Ecosystem/Bumble/Daily-Notes/2026-01-05.md|Routed: Bumble Docs]]
- [[content/Categories/Docs/Star-Sailors-Ecosystem/Experiment1/Daily-Notes/2026-01-05.md|Routed: Experiment1 Docs]]
- [[content/Categories/Docs/Weathr/Daily-Notes/2026-01-05.md|Routed: Weathr Docs]]
