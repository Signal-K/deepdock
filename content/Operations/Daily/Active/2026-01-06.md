---
title: Daily Dashboard
date: 2026-01-06
tags:
  - dashboard
  - daily
icon: lucide//layout-dashboard
---

# 🎯 Daily Dashboard - 2026-01-06

## � Navigation

```dataviewjs
// Get current date from filename
const currentDate = dv.current().file.name;

if (currentDate && currentDate.match(/^\d{4}-\d{2}-\d{2}$/)) {
  // Get all daily files and extract dates from filenames
  const dailyFiles = dv.pages('"content/Operations/Daily/Active"')
    .filter(p => p.file.name.match(/^\d{4}-\d{2}-\d{2}$/))
    .map(p => ({ date: p.file.name, path: p.file.path }))
    .sort((a, b) => a.date.localeCompare(b.date));
  
  const currentIndex = dailyFiles.findIndex(f => f.date === currentDate);
  
  let prevButton = "⬅️ Previous";
  let nextButton = "Next ➡️";
  
  if (currentIndex > 0) {
    const prevFile = dailyFiles[currentIndex - 1];
    prevButton = `[⬅️ ${prevFile.date}](${prevFile.path.replace('content/', '')})`;
  }
  
  if (currentIndex < dailyFiles.length - 1) {
    const nextFile = dailyFiles[currentIndex + 1];
    nextButton = `[${nextFile.date} ➡️](${nextFile.path.replace('content/', '')})`;
  }
  
  dv.paragraph(`${prevButton} | ${nextButton}`);
} else {
  dv.paragraph("⬅️ Previous | Next ➡️");
}
```

## �🚀 Quick Actions

```button
name ➕ Create
type append template
action Create-Task
templater true
class inline
```
```button
name 📋 Organize
type append template
action Organize-Daily
templater true
class inline
```
```button
name 📅 Archive
type append template
action Archive-Daily
templater true
class inline
```
```button
name 🔄 Reset
type append template
action Reset-Dashboard
templater true
class inline
```


```button
name 📊 Projects
type link
action obsidian://open?vault=quartz&file=content/Categories/_Index
class inline
```

---


## 🗓️ Today's Focus Tasks

```dataviewjs
const today = dv.current().date ? dv.current().date.toString().split('T')[0] : null;
if (!today) {
  dv.paragraph('No date set for this page.');
} else {
  let tasks = [];
  for (let page of dv.pages('"content/Categories"')) {
    if (page.file && page.file.tasks) {
      for (let t of page.file.tasks) {
        if (t.text.includes(`#${today}`)) {
          tasks.push(t);
        }
      }
    }
  }
  if (tasks.length === 0) {
    dv.paragraph('No tasks tagged for today.');
  } else {
    dv.taskList(tasks, false);
  }
}
```

## 🧩 In-Progress Story Tasks

```dataviewjs
// Stories in progress with no tasks
// Find story pages under content/Categories and filter those marked in-progress
const pages = dv.pages("\"content/Categories\"").filter(p => p.file && p.file.path && p.file.path.includes('/Stories/'));
const allStories = pages.filter(p => p.status && String(p.status).toLowerCase() === 'in-progress');

// Collect pages that actually have tasks
const withTasks = new Set(pages.filter(p => p.file && p.file.tasks).map(p => p.file.path));
const noTaskStories = allStories.filter(p => !withTasks.has(p.file.path));

dv.header(3, "📝 In-Progress Stories (No Tasks)");
if (!noTaskStories || noTaskStories.length === 0) {
  dv.paragraph("No in-progress stories without tasks.");
} else {
  dv.header(4, "Stories in progress (no tasks defined):");
  noTaskStories.forEach(p => dv.paragraph(p.file.link));
}
```

## 📅 Recent

```note-gallery
path: content/Operations/Daily/Active
query: 'path:content/Operations/Daily/Active/'
recursive: false
limit: 4
sort: desc
sortBy: mtime
fontSize: 8pt
showTitle: true
breakpoints:
  default: 4
  1500: 3
  1000: 2
  700: 1
```

---

## �📥 Today's Notes Dump

### Quick Thoughts
Add a button to find all incomplete tasks that had a due date of the last week and assign them to today.



### Meetings & Conversations
Have a meeting with Fred at Post at 10, I don't think I'm going to put any time beforehand (i.e. while I'm at QV1) on any real planning....I'll just keep working on #Bumble and maybe do a little minor fleshing out of this idea I had yesterday.

What's my main problem with Stardew Valley? Well, from a narrative perspective...I don't like backwards "hick towns" or the people in them. Also not really a fan of the anti-corporate message i.e. "corporate jobs are all evil and soul-sucking". Not all of the characters are *that* likeable. 
But from a gameplay perspective...
1. Not enough spaceships
2. Not much industry
3. It gets repetitive a bit - and can be very grindy. It can be a little hard to figure out what to do.....and a lot of the skills or resources are very frustrating to acquire

The idea from [[2026-01-05]] was that you could combine MSM mechanics with a cozy game like SV.
Having singing characters would be a bit of a rip-off...adding a musical element [noteblocks?] is one route. I'm not really sure how this would work...but I'm sure Fred could figure out a solution (or rather, how close we could go to plagiarism....👀).
The mechanics of something like MSM that are good -
1. Collectibles, breeding...(merging)
2. Playful characters with enough differences to 'justify' collecting everything?
3. Immense decoration capacity



### Ideas & Brainstorming




---

## Routed Notes
- [[content/Categories/Ideas/Star-Sailors-Ecosystem/Bumble/Daily-Insights/2026-01-06.md|Routed: Bumble Ideas]]
- [[content/Categories/Docs/General-Operations/Daily-Notes/2026-01-06.md|Routed: General-Operations Docs]]
- [[content/Categories/Docs/Star-Sailors-Ecosystem/Bumble/Daily-Notes/2026-01-06.md|Routed: Bumble Docs]]
