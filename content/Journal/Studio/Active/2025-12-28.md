---
title: Daily Notes
date: 2025-12-28
tags:
  - daily
  - archive
icon: lucide//calendar-days
---

# 📅 Daily Notes - 2025-12-28

## � Navigation

```dataviewjs
// Get current date from filename
const currentDate = dv.current().file.name;

if (currentDate && currentDate.match(/^\d{4}-\d{2}-\d{2}$/)) {
  // Get all daily files and extract dates from filenames
  const dailyFiles = dv.pages('"Operations/Daily/Active"')
    .filter(p => p.file.name.match(/^\d{4}-\d{2}-\d{2}$/))
    .map(p => ({ date: p.file.name, path: p.file.path }))
    .sort((a, b) => a.date.localeCompare(b.date));
  
  const currentIndex = dailyFiles.findIndex(f => f.date === currentDate);
  
  let prevButton = "⬅️ Previous";
  let nextButton = "Next ➡️";
  
  if (currentIndex > 0) {
    const prevFile = dailyFiles[currentIndex - 1];
    prevButton = `[⬅️ ${prevFile.date}](${prevFile.path.replace('content/', '')})`;
  }
  
  if (currentIndex < dailyFiles.length - 1) {
    const nextFile = dailyFiles[currentIndex + 1];
    nextButton = `[${nextFile.date} ➡️](${nextFile.path.replace('content/', '')})`;
  }
  
  dv.paragraph(`${prevButton} | ${nextButton}`);
} else {
  dv.paragraph("⬅️ Previous | Next ➡️");
}
```

## �💭 Quick Thoughts

Something I want to do is to get Copilot to propose everything I should do this week during the "drawing periods".

I'm still finishing up organising the tasks, which will help organise my days.
The pens will make a comeback tomorrow!

Once I have the tasks organised, I'm going to conduct some research into a cross-platform clone of SuperLocal




## 🗣️ Meetings & Conversations






## 💡 Ideas & Brainstorming






---

*Archived from Daily Dashboard on 2025-12-28*


- [[Projects/Projects/Other-Projects/Ideas/Map-App/Daily-Insights/2025-12-28.md|Routed: Map-App Ideas]]
- [[Projects/Projects/Other-Projects/Docs/Map-App/Daily-Notes/2025-12-28.md|Routed: Map-App Docs]]
