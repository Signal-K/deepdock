---
title: Daily Notes
date: 2025-12-29
tags:
  - daily
  - archive
icon: lucide//calendar-days
---

# 📅 Daily Notes - 2025-12-29

## � Navigation

```dataviewjs
// Get current date from filename
const currentDate = dv.current().file.name;

if (currentDate && currentDate.match(/^\d{4}-\d{2}-\d{2}$/)) {
  // Get all daily files and extract dates from filenames
  const dailyFiles = dv.pages('"Operations/Daily/Active"')
    .filter(p => p.file.name.match(/^\d{4}-\d{2}-\d{2}$/))
    .map(p => ({ date: p.file.name, path: p.file.path }))
    .sort((a, b) => a.date.localeCompare(b.date));
  
  const currentIndex = dailyFiles.findIndex(f => f.date === currentDate);
  
  let prevButton = "⬅️ Previous";
  let nextButton = "Next ➡️";
  
  if (currentIndex > 0) {
    const prevFile = dailyFiles[currentIndex - 1];
    prevButton = `[⬅️ ${prevFile.date}](${prevFile.path.replace('content/', '')})`;
  }
  
  if (currentIndex < dailyFiles.length - 1) {
    const nextFile = dailyFiles[currentIndex + 1];
    nextButton = `[${nextFile.date} ➡️](${nextFile.path.replace('content/', '')})`;
  }
  
  dv.paragraph(`${prevButton} | ${nextButton}`);
} else {
  dv.paragraph("⬅️ Previous | Next ➡️");
}
```

## �💭 Quick Thoughts

For the daily focus tasks, I'm going to need to figure out a way to automatically sort all the documentation along with each ticket - creating rules to match the ticket id with the documentation.

The focus for now is on Bumble, Crashlandings, ChopDot, SuperLocal clone I think. Coral clone will start development after Bumble. I'm happy with the rough plan so far.

Let's update the dashboard "reset" script to assign an icon to the dashboard file.

I'm trying to think of everything else in the scope of the projects, I think this is everything. Next week everyone will be coming back from holidays?? so that's the time to start distributing to contacts [[People to reach out to]]. However, I should at least start planning this campaign at some point this week.


## 🗣️ Meetings & Conversations






## 💡 Ideas & Brainstorming

At some point, we're going to need to look into adding the ability to add more plots to existing farms for #Bumble . AND deciding on the terminology, too.




---

*Archived from Daily Dashboard on 2025-12-29*


- [[Projects/Projects/Other-Projects/Ideas/Map-App/Daily-Insights/2025-12-29.md|Routed: Map-App Ideas]]
- [[Projects/Projects/Star-Sailors/Docs/Star-Sailors-Ecosystem/Bumble/Daily-Notes/2025-12-29.md|Routed: Bumble Docs]]
- [[Projects/Projects/Star-Sailors/Docs/Star-Sailors-Ecosystem/Experiment1/Daily-Notes/2025-12-29.md|Routed: Experiment1 Docs]]
- [[Projects/Projects/Other-Projects/Docs/Map-App/Daily-Notes/2025-12-29.md|Routed: Map-App Docs]]
