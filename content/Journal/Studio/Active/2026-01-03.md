---
title: Daily Dashboard
date:
  "{ date:YYYY-MM-DD }":
tags:
  - dashboard
  - daily
icon: lucide//layout-dashboard
sticker: lucide//calendar-x2
---

# 🎯 Daily Dashboard - {{date:YYYY-MM-DD}}

## � Navigation

```dataviewjs
// Get current date from filename
const currentDate = dv.current().file.name;

if (currentDate && currentDate.match(/^\d{4}-\d{2}-\d{2}$/)) {
  // Get all daily files and extract dates from filenames
  const dailyFiles = dv.pages('"Operations/Daily/Active"')
    .filter(p => p.file.name.match(/^\d{4}-\d{2}-\d{2}$/))
    .map(p => ({ date: p.file.name, path: p.file.path }))
    .sort((a, b) => a.date.localeCompare(b.date));
  
  const currentIndex = dailyFiles.findIndex(f => f.date === currentDate);
  
  let prevButton = "⬅️ Previous";
  let nextButton = "Next ➡️";
  
  if (currentIndex > 0) {
    const prevFile = dailyFiles[currentIndex - 1];
    prevButton = `[⬅️ ${prevFile.date}](${prevFile.path.replace('content/', '')})`;
  }
  
  if (currentIndex < dailyFiles.length - 1) {
    const nextFile = dailyFiles[currentIndex + 1];
    nextButton = `[${nextFile.date} ➡️](${nextFile.path.replace('content/', '')})`;
  }
  
  dv.paragraph(`${prevButton} | ${nextButton}`);
} else {
  dv.paragraph("⬅️ Previous | Next ➡️");
}
```

## �🚀 Quick Actions

```button
name ➕ Create
type append template
action Create-Task
templater true
class inline
```

 
```button
name 📋 Organize
type append template
action Organize-Daily
templater true
class inline
```
```button
name 📅 Archive
type append template
action Archive-Daily
templater true
class inline
```
```button
name 🔄 Reset
type append template
action Reset-Dashboard
templater true
class inline
```




```button
name 📊 Projects
type link
action obsidian://open?vault=content&file=Projects/index
class inline
```

---


## 🗓️ Today's Focus Tasks

```dataviewjs
const filename = dv.current().file.name;
const today = filename.match(/\d{4}-\d{2}-\d{2}/)?.[0];
if (!today) {
  dv.paragraph('Could not determine date from filename.');
} else {
  let tasks = [];
  for (let page of dv.pages('"content/Projects"')) {
    if (page.file && page.file.tasks) {
      for (let t of page.file.tasks) {
        if (t.text.includes(`#${today}`)) {
          tasks.push(t);
        }
      }
    }
  }
  if (tasks.length === 0) {
    dv.paragraph('No tasks tagged for today.');
  } else {
    dv.taskList(tasks, false);
  }
}
```

## 🧩 In-Progress Story Tasks

```dataviewjs
// Stories in progress with no tasks
// Find story pages under content/Categories and filter those marked in-progress
const pages = dv.pages("\"content/Categories\"").filter(p => p.file && p.file.path && p.file.path.includes('/Stories/'));
const allStories = pages.filter(p => p.status && String(p.status).toLowerCase() === 'in-progress');

// Collect pages that actually have tasks
const withTasks = new Set(pages.filter(p => p.file && p.file.tasks).map(p => p.file.path));
const noTaskStories = allStories.filter(p => !withTasks.has(p.file.path));

dv.header(3, "📝 In-Progress Stories (No Tasks)");
if (!noTaskStories || noTaskStories.length === 0) {
  dv.paragraph("No in-progress stories without tasks.");
} else {
  dv.header(4, "Stories in progress (no tasks defined):");
  noTaskStories.forEach(p => dv.paragraph(p.file.link));
}
```

## 📅 Recent

```note-gallery
path: Operations/Daily/Active
query: 'path:Operations/Daily/Active/'
recursive: false
limit: 4
sort: desc
sortBy: mtime
fontSize: 8pt
showTitle: true
breakpoints:
  default: 4
  1500: 3
  1000: 2
  700: 1
```

---

## �📥 Today's Notes Dump

### Quick Thoughts
Okay, so I finally fixed the issue (I think) with the task list and the 29/12/2025 date error.
DUNS number for A&S is upcoming, I just need to wait for aswatch.ch domain to be activated through netim. 
Update (two mins later) - domain is registered and activated. Guess I'll do the email now, then
![[Pasted image 20260103083844.png]]

Going to go through the stories for #Bumble now. Getting it pushed out today - committed to this deadline.

I think it would be good to have some form of block like "thing to look in to/conceptualise" and set a date; then when I open a daily file that day I see it. We'll see...

There is a lot of admin work I have to do in the repo, including splitting up the styling from the functional components, removing unused functionality, etc...not really labelling this under a specific task, though. A starting point would be to organise the `lib` directory here.
### Meetings & Conversations
One other thing I need to do is have a meeting with Rhys, and so I'll quickly write up some things I think should be at the forefront of the discussion...
1. No signups for Star Sailors since 25/12, dwindling visits even below the pre-PH launch....
2. The newsletter thing - what's the key messaging to get as many answers as possible?
3. Thoughts on my feedback ideas for launch of #Bumble - [[#^51f437]]
Will go into more detail on this with the [[Big async doc]]



### Ideas & Brainstorming
Down the line (see [[Pollination to occur at regular intervals]]), we need to have a slightly more complex/valued "pollination score" factor. But 10 full harvests for one bee is fine for now - as it's still a relatively high barrier for each bee...

I think the tasks I've defined now are pretty sufficient, so the rollout strategy would be to deploy to Vercel, and then get feedback on the following things:
1. What type of tutorial context do we need? ^51f437
2. Thoughts on assets, layout, and the svgs that should be replaced by manual/real drawn assets (e.g. plots)
3. Timing?
4. Is the flow fun? 

I should have a think, once this is done, about comparing it to Stardew Valley and similar farming games to determine the depth/gap in gameplay...obviously the perspective and character development is very different...but there's still a lot more things to do.
This leads me to think that the coral game could actually be combined in the user experience, but obviously (right now) I'm building Bumble in RN and proposing the coral game in Godot. 
I need to get these builds out faster....but I think clearly labelling the feedback I'm after is a good thing.  ^fbb0d2


Now that the holidays are technically over, I'm also going to get onto that email to everyone for #Star-Sailors-web as well.

One more thing - adding the vercel action/runner on Github for pushes to `main`/`master`

Been having a think about what the next step should be for #Crashlandings . I think I should create a list of bugs or things that would need polishing e.g. the #CharacterMovement. Obviously the coral minigame doesn't really fit into the sort of perspective/character style that the SV clone has...but like I was saying here [[#^fbb0d2]] once we get the build out we take a look at what to do next with #Bumble. Doesn't really fit in with the theme for the whole #godot-mars idea I was running with for those few weeks last month/year but you never know. I guess I should look at every possible type of game or project that could be made with the SV sort of perspective and have a think about what would 1) be most fun to build and 2) help with user acquisition the most. 


Update from me (end of day) - so wrote almost all of the code myself manually which chewed up a fair bit of time, but provided I don't forget my pencilcase tomorrow I should be able to do more brainstorming around the new stuff and take a look at what should come next. Bee sprite didn't get done unfortunately - that's on the agenda for MRKT.

---


- [[Projects/Projects/Star-Sailors/Ideas/Star-Sailors-Ecosystem/Bumble/Daily-Insights/2026-01-03.md|Routed: Bumble Ideas]]

- [[Projects/Projects/Star-Sailors/Ideas/Star-Sailors-Ecosystem/Click-A-Coral/Daily-Insights/2026-01-03.md|Routed: Click-A-Coral Ideas]]

- [[Projects/Projects/Star-Sailors/Ideas/Star-Sailors-Ecosystem/Godot-Mars-Archive/Daily-Insights/2026-01-03.md|Routed: Godot-Mars-Archive Ideas]]
- [[Projects/Docs/General-Operations/Daily-Notes/2026-01-03.md|Routed: General-Operations Docs]]
- [[Projects/Projects/Star-Sailors/Docs/Star-Sailors-Ecosystem/Bumble/Daily-Notes/2026-01-03.md|Routed: Bumble Docs]]
- [[Projects/Projects/Star-Sailors/Docs/Star-Sailors-Ecosystem/Star-Sailors-Web/Daily-Notes/2026-01-03.md|Routed: Star-Sailors-Web Docs]]
